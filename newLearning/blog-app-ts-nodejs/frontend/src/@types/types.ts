export interface BlogType{
    title: string,
    content: string,
    createdBy: string
}

export interface LoginPageProps {
    isLoginPage: boolean;
}

export interface ButtonProps{
    name: string,
    path: string
}